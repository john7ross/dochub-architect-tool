# DocHub Architect Tool dependencies
# Python 3.10+
#
# ASCII only, on purpose: pip reads this file in the console code page, and a
# non-UTF-8 console turns a comment in Cyrillic into a UnicodeDecodeError
# before a single package is installed.

# The MCP server itself.
# SDK 2.x renamed FastMCP to MCPServer; both are supported, the import decides
# at runtime, so no upper bound here
mcp>=1.2.0
pydantic>=2.0.0

# YAML.
# ruamel is used when editing repository files: it keeps the comments and the
# key order that people read
ruamel.yaml>=0.17.0
PyYAML>=6.0.0

# Executing the DocHub metamodel from vendor/dochub
jsonata-python>=0.3.0

# Creating a merge request through the GitLab API.
# git itself is called through its CLI: one dependency less when the project
# is handed over as an archive
httpx>=0.27.0

# Root certificates for setup_assets.py: the Windows store may carry an expired
# root, and the download then fails while verifying the chain. It arrives with
# httpx anyway, declared explicitly because the script depends on it directly
certifi

# Development
pytest>=7.4.0
